import { createFileRoute } from "@tanstack/react-router";
import { Star } from "lucide-react";
import { Reveal } from "@/components/reveal";
export const Route = createFileRoute("/reviews")({ head: () => ({ meta: [{ title: "Client Reviews — Ali Properties Lahore" }, { name: "description", content: "Read why clients rate Ali Properties 5.0 on Google." }, { property: "og:title", content: "Client Reviews — Ali Properties" }, { property: "og:description", content: "5.0 rated property advice in Lahore." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Reviews });
function Reviews() { const reviews = [{q:"Professional, honest and extremely responsive. The entire transaction was handled with remarkable care.",n:"Lahore Homebuyer"},{q:"Ali understood exactly what we were looking for and never wasted our time. A rare level of service.",n:"Private Client"},{q:"Clear advice, excellent local knowledge and complete support from first viewing through completion.",n:"Property Investor"}]; return <div className="min-h-screen bg-secondary px-5 pb-28 pt-36 text-secondary-foreground sm:px-8 lg:px-12"><div className="mx-auto max-w-[1500px]"><div className="grid gap-12 lg:grid-cols-2"><div><p className="editorial-label text-primary">Client notes</p><h1 className="mt-6 text-7xl sm:text-9xl">5.0</h1><div className="mt-4 flex gap-2 text-primary" aria-label="5 out of 5 stars">{Array.from({length:5}).map((_,i) => <Star key={i} className="size-5 fill-current" />)}</div><p className="mt-5 text-sm text-secondary-foreground/60">Based on 6 Google reviews</p></div><p className="self-end font-serif text-4xl leading-tight sm:text-6xl">Quiet confidence.<br /><span className="italic text-primary">Lasting trust.</span></p></div><div className="mt-24 grid gap-px bg-secondary-foreground/15 lg:grid-cols-3">{reviews.map((review,index) => <Reveal key={review.n} className="flex min-h-96 flex-col justify-between bg-secondary p-8 sm:p-10"><span className="font-serif text-6xl text-primary/45">“</span><blockquote className="font-serif text-3xl leading-snug">{review.q}</blockquote><div><p className="editorial-label text-primary">{review.n}</p><p className="mt-2 text-xs text-secondary-foreground/50">Google Review 0{index+1}</p></div></Reveal>)}</div></div></div>; }